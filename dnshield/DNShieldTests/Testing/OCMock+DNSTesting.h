#import <OCMock/OCMock.h>

// Placeholder helper header for DNShield tests. Historically this exposed
// project-specific additions to OCMock; for now we simply ensure a stable
// include path so existing imports keep working even if no extra utilities are
// required.
